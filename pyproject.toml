[project]
name = "api-test"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Rishabh Verma", email = "inhall.updatebackup@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "currencyconverter>=0.18.21",
    "flask>=3.1.3",
    "frozen-flask>=1.0.2",
    "requests>=2.34.2",
]

[project.scripts]
api-test = "api_test:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
